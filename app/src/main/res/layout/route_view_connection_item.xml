<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/connection_view_layout">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp">

        <ImageView
            android:id="@+id/connection_mode_logo"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_alignParentLeft="true"
            android:layout_alignParentTop="true"
            app:srcCompat="@drawable/ic_bus" />

        <View
            android:id="@+id/connection_path_view"
            android:layout_width="match_parent"
            android:layout_height="5dp"
            android:background="#000"
            android:layout_marginRight="1dp"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@+id/connection_mode_logo" />

        <ImageView
            android:id="@+id/last_connection_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_marginTop="8dp"
            app:srcCompat="@drawable/ic_circle"
            android:visibility="invisible" />

    </RelativeLayout>

    <TextView
        android:id="@+id/connection_name"
        android:paddingLeft="@dimen/dp_0_3"
        android:paddingRight="@dimen/dp_0_3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/busPrimary"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="@dimen/dp_0_5"
        android:visibility="gone"
        android:textColor="@color/primaryColor"
        android:textSize="12dp"
        android:textStyle="bold" />

</RelativeLayout>